package br.ufba.matc89;

import android.os.Bundle;
import android.support.v4.app.Fragment;

public interface MyTransactionListener {
	public void showFragment(int container, Fragment fragment, String tag, Fragment lastFragment, boolean addToBackStack, boolean onBackStackTop);
	//public void showFragment(int container, Fragment fragment, String tag, String lastTag, boolean addToBackStack){
	//FragmentManager fragmentManager = getSupportFragmentManager();
    //FragmentTransaction transaction = fragmentManager.beginTransaction();

    //if ( lastTag != null && !lastTag.equals("")) {
    //    Fragment lastFragment = fragmentManager.findFragmentByTag( lastTag );
    //    if ( lastFragment != null ) {
    //        transaction.hide( lastFragment );
    //    }
    //}
	//
    //if ( fragment.isAdded() ) {
    //    transaction.show( fragment );
    //}
    //else {
    //    transaction.add( container, fragment, tag );
    //}

    //if ( addToBackStack ) {
    //    transaction.addToBackStack( tag );
    //}
	//
    //transaction.commit();
	//
    // set the active tag
    //activeFragTag = tag;
	//}
}
